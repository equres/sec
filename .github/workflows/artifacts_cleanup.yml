name: Artifacts Cleanup
on:
  push:
    branches:
      - '*'

jobs:
  delete-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: kolpav/purge-artifacts-action@v1
        with:
          token: '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'
          expire-in: 0