{{ template "base" .}}

{{ define "head"}}
    <title>Equres > Download Stats</title>
{{ end }}

{{ define "content"}}

    <table class="table">
        <thead>
            <tr>
                <th style="font-size: 0.9em;">Date\Time</th>
                {{ range $Hour := .Hours }}
                    <th style="font-size: 0.9em;">{{ $Hour }}</th>
                {{ end }}
            </tr>
        </thead>
        <tbody>
            {{ range $Date, $Stats := .DownloadStats }}
                <tr>
                    <td style="font-size: 0.9em;">{{ $Date }}</td>
                    {{ range $DateStats := $Stats }}
                        <td style="color: {{ StatsCSSColor $DateStats.FilesDownloaded }}; font-size: 0.9em;">{{ $DateStats.FilesDownloaded }}</td>
                    {{ end }}
                </tr>
            {{ end }}
        </tbody>
    </table>
{{ end }}